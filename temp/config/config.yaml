composition:
  name: KVQueue
systems:
  queue:
    type: SimpleQueue
    delivery: at_least_once
    max_size: 10

  kv:
    type: SimpleKV
    consistency: strong
    conditional_writes: true
    idempotent_writes: true
    max_size: 10
