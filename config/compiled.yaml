composition:
  name: KVQueue
systems:
  queue:
    type: BaseQueue
    delivery: at_least_once
    ordering: first_in_first_out
    max_size: 100
  kv:
    type: BaseKV
    consistency: strong
    conditional_writes: true
    idempotent_writes: true
    replicas: false
    max_size: 100
